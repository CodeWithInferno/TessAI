from .filesystem_manager import FileMemory
from .llm import llm, embedding


class Tess:
    @staticmethod
    def rag_chain_invoke(query: str):
        # Placeholder logic for now
        return f"I received: {query}"
    

